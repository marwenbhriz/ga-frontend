
---
apiVersion: apps/v1
kind: Deployment               
metadata:
  name: ga-frontend          
spec:
  replicas: 3                    
  selector:
    matchLabels:
      app: ga-frontend       # This deployment applies to any Pods matching the specified label
  template:                      # This deployment will create a set of pods using the configurations in this template
    metadata:
      labels:                    # The labels that will be applied to all of the pods in this deployment
        app: ga-frontend
    spec:                        # Spec for the container which will run in the Pod
      containers:
      - name: ga-frontend
        image: marbenhriz/ga-frontend:latest
        imagePullPolicy: IfNotPresent
        ports:
          - containerPort: 9090  
        livenessProbe:           
          httpGet:
            path: /health
            port: 9090
            scheme: HTTP
          initialDelaySeconds: 5
          periodSeconds: 15
          timeoutSeconds: 5
        readinessProbe:         
          httpGet:
            path: /readiness
            port: 9090
            scheme: HTTP
          initialDelaySeconds: 5
          timeoutSeconds: 1 